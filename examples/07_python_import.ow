// OwlLang: Python Interop
// Demonstrates importing and using Python modules

from python import math

fn main() {
    let x = 16.0
    let root = math.sqrt(x)
    let pi = math.pi

    print(root)
    print(pi)
}
